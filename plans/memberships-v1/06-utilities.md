# Phase 6: Utilities

## Status: SKIPPED

### Reason

No complex client-side calculations needed. Formatting (currency, dates) uses existing foundation utils. The backend handles expiry calculation and days_remaining.

### Notes

- `formatCurrencyINR` from foundation module handles price display
- `RelativeTime` component handles date display
- Plan type labels ("Monthly" / "Annual") are simple enough to inline
