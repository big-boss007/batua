# Phase 5: Scheduler

SKIPPED -- No schedulers in this phase. Campaign activation/deactivation is time-based via SQL queries (starts_at/ends_at filtering). Credit expiry tracking is via system_health queries.
