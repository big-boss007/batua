# Phase 1: Remote -- SKIPPED

No external API calls were required for Phase 1 services. The Shopify discount application in the redemption flow is currently a stub (`apply_shopify_discount` returns `stub-discount-{id}`), to be implemented when the Shopify connector is built.
