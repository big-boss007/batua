# Storage

## Status: SKIPPED

### Reason

Verbatim port. Each service's `storage.rs` is copied unchanged. No new
SQL queries, no schema redesign. See `10-database.md` for the migration
parity check that exercises the storage layer end-to-end.
