# Handlers

## Status: SKIPPED

### Reason

Verbatim port. Each service's `handler.rs` is copied unchanged. Handler
parity is verified end-to-end via `cargo test` and the smoke `cargo run`
in the new repo (see `checklist.md`).
