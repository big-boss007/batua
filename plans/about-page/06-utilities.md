# Phase 6: Utilities

## Status: SKIPPED

### Reason
The only helper is a one-line `openWebsite(path)` that calls `window.open(path,
'_blank')` — kept component-local in `+page.svelte`. No shared utilities needed.

### Notes
- This phase was evaluated and intentionally skipped.
